apply plugin: 'java-library'

ext.moduleName = 'PriceListObserver.Adapter'

dependencies {
    implementation(
            project(':Domain'),
            project(':Config'),
            project(':UseCase')
    )
}