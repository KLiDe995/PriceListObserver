apply plugin: 'java'

ext.moduleName = 'PriceListObserver.Adapter'

dependencies {
    implementation(
            project(':Domain'),
            project(':Config'),
            project(':UseCase')
    )
}