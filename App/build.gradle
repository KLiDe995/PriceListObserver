apply plugin: 'java'

ext.moduleName = 'PriceListObserver.App'

repositories {
    mavenCentral()
}

dependencies {
    implementation(
            project(':Domain'),
            project(':UseCase')
    )
}