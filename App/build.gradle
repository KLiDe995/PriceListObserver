apply plugin: 'java'

ext.moduleName = 'PriceListObserver.App'

dependencies {
    implementation(
            project(':Adapter'),
            project(':Framework'),
            project(':Config')
    )
}